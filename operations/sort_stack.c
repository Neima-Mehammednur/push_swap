#include "push_swap.h"

//Once you've parsed the input and created your initial stack, 
// call the calculate_costs function to evaluate the costs for moving
// elements from the input stack to the target stack. Use the results from 
// calculate_costs to inform your sorting logic. The main sorting function 
// can loop through the elements in the stack, using the calculated costs to 
// decide which moves to make.
void sort_3(t_stack **a)
{
    // Sorting logic for 3 elements
}

void sort_5(t_stack **a, t_stack **b)
{
    // Sorting logic for 5 or fewer elements
}

void big_sort(t_stack **a, t_stack **b)
{
    // Sorting logic for more than 5 elements
}
